- hosts: localhost
  connection: local
  become: yes
  tasks:
    - name: Installing..
      apt:
        name: "{{item}}"
        state: present
      with_items:
        - htop
        - ncdu
        - apache2
    - name: Configuring webserver
      raw: echo "hello world" > /var/www/html/index.html
        
